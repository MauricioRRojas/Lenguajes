#include "main.h"


int main()
{
    /*FILE "gramar" = fopen("gramatica.txt","r");
    if(gramar == NULL)
    {
        perror("Error opening the file\n");
        return EXIT_FAILURE;
    }
    Node *head = createLinkedList(gramar);
    fclose(gramar);
    printflist(gramar);
    freeLinkedList(head);
    */
    return EXIT_SUCCESS;
}